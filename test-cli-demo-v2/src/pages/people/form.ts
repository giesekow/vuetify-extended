// Generated by vuetify-ext create report
import { $FD, $FM, $PT, type ReportMode } from 'vuetify-extended';

export function createPeopleForm(mode: ReportMode = 'display') {
  return $FM(
    {
      title: { key: "pages.people.form.title", fallback: "People Workspace Form" },
      mode,
      width: 920,
    },
    {
      children: () => [
        // vuetify-ext:form-parts
        $PT(
          { cols: 12, dense: true },
          {
            children: () => [
              // vuetify-ext:part:1:fields
              // vuetify-ext:field:email
              $FD({
                label: { key: "pages.people.form.fields.email.label", fallback: "Email" },
                storage: "email",
                cols: 6,
                required: true,
              }),
              $FD({
                label: { key: "pages.people.form.fields.name.label", fallback: "Name" },
                storage: 'name',
                cols: 6,
                required: true,
              }),
              $FD({
                label: { key: "pages.people.form.fields.code.label", fallback: "Code" },
                storage: 'code',
                cols: 6,
              }),
              $FD({
                label: { key: "pages.people.form.fields.description.label", fallback: "Description" },
                storage: 'description',
                type: 'textarea',
                cols: 12,
              }),
              $FD({
                type: 'label',
                label: { key: "pages.people.form.hints.replaceSchema", fallback: "Replace these starter fields with the real form schema for this page." },
                cols: 12,
              }),
            ],
          },
        ),
      ],
    },
  );
}
