// Generated by vuetify-ext create form
import { $FD, $FM, $PT, type ReportMode } from 'vuetify-extended';

export function createPeopleForm2(mode: ReportMode = 'display') {
  return $FM(
    {
      title: "People Contact Details",
      mode,
      width: 920,
    },
    {
      children: () => [
        // vuetify-ext:form-parts
        $PT(
          { cols: 12, dense: true },
          {
            children: () => [
              // vuetify-ext:part:1:fields
              $FD({
                label: 'Name',
                storage: 'name',
                cols: 6,
                required: true,
              }),
              $FD({
                label: 'Code',
                storage: 'code',
                cols: 6,
              }),
              $FD({
                label: 'Description',
                storage: 'description',
                type: 'textarea',
                cols: 12,
              }),
              $FD({
                type: 'label',
                label: 'Replace these starter fields with the real form schema for this page.',
                cols: 12,
              }),
            ],
          },
        ),
      ],
    },
  );
}
